# 贡献指南

感谢你愿意为 MoeApk App 的 UX 出一份力！本页是流程规范；**技术约定（架构、禁区、走查清单）统一见 [AGENTS.md](AGENTS.md)**——它和 AI 工具读的是同一份，人类贡献者同样适用。

## 贡献方式

1. **Fork** 本仓库并 clone 到本地；
2. 从 `master` 切一个分支（如 `fix-xxx` / `design-xxx`）；
3. 修改 → 本地走查（明/暗、横/竖屏、`file://` 直开）；
4. 提交（中文提交信息：`<类型>: <简述>`，类型如 `原型:` `修复:` `调整:`）；
5. 向本仓库 `master` 发 **Pull Request**。

## 两种典型贡献

- **改进点**：现有页面的 bug、细节优化、交互改进 —— 直接提 PR；
- **新设计**：新页面、新流程、大改版 —— **先开 Issue 或到 Discussions 讨论**，定方向后再动手。

## 用 AI 辅助贡献

欢迎且推荐（见 README「用 AI Agent 修改原型」）。要求：

- 把仓库根目录的 `AGENTS.md` 交给你的 AI 工具作为项目上下文（主流工具会自动读取）；
- PR 描述中注明使用了 AI 辅助，并**确保你自己理解每一处改动、能在评审中解释**；
- AI 产出必须经过你人工走查（明/暗、横/竖屏）再提交。

## PR 评审标准

- 只改与描述相符的内容，一个 PR 只做一件事；
- 不违反 AGENTS.md 的「禁区」（零依赖、零构建、`file://` 可用、不碰品牌素材等）；
- 附改动说明与截图（明/暗主题至少各一张）；
- 符合现有设计语言（设计令牌、M3 风格、既有交互范式）。

## 数据说明

`js/data/` 是内嵌演示数据，贡献者可直接修改；维护者会不定期从线上站点数据同步刷新（内部流程，无需关心）。
